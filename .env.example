# Copy this file to .env and fill in your token.
# .env is git-ignored. Load it with: uv run --env-file .env aipme <command>

# Hetzner Cloud Console -> your project -> Security -> API tokens (Read & Write)
HCLOUD_TOKEN=
